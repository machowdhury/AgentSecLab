# Phase 17D — Instructor acceptance

Instructor guide: [INSTRUCTOR_GUIDE.md](INSTRUCTOR_GUIDE.md).

Can start (`lab-up`), rebuild (`--build`), restage XML (`--refresh-app`), explain architecture, launch known experiments, teach LIVE vs REPLAY, Path A/B, actual PDP, debrief limitations, troubleshoot missing evidence.

Remaining instructor-only assumptions:

- Docker Desktop resource settings
- This workstation already had Splunk data and models
- `lab-ready.sh` as a single wrapper was not captured in this session after the Home/Mastery REST additions (views still HTTP 200)

No hidden privileged learner mode.
