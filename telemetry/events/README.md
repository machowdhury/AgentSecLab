Predecessor representative events (`normal_request`, `prompt_attack`, tool/A2A/memory/RAG/chain) were removed in Phase 2A.

First-lab emitters use schema 1.0.0 names only. See `schemas/security_event.schema.json` and live `artifacts/<run-id>/events.jsonl`.
